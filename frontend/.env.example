# Configuration de l'URL du Backend

# IMPORTANT: Cette URL est utilisée par le NAVIGATEUR, pas par le conteneur frontend
# Elle doit donc pointer vers une adresse accessible depuis votre machine hôte

# Phase 1 (docker run):
# Remplacer localhost par l'IP de votre machine (ex: 192.168.1.100)
# VITE_API_URL=http://192.168.1.100:8000

# Phase 2/3 (docker-compose):
# Si vous accédez au frontend depuis localhost, gardez localhost
VITE_API_URL=http://localhost:8000

# Note: Les variables Vite doivent commencer par VITE_ pour être exposées au code frontend
